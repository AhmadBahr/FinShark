import React from 'react'
import Hero from '../../Components/Hero/Hero.tsx'

interface Props { }

const Home = (props: Props) => {
    return (
        <div>
            <Hero />
        </div>
    )
}

export default Home